policy:
  module: "sandbox.policies.grid_search"
  args:
    samples_per_dim: 5
controls:

  - module: "sandbox.controls.pose"
    rotation_X: 0
    rotation_Y: (0, 0.5)

  - module: "sandbox.controls.exposition"

  - module: "sandbox.controls.material_switch"


